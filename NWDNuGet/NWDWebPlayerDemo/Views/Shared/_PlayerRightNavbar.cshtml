@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@using Microsoft.AspNetCore.Http
@using Microsoft.AspNetCore.Builder
@using Microsoft.AspNetCore.Mvc.Localization
@using Microsoft.Extensions.Options
@using NWDFoundation.Facades
@using NWDWebRuntime.Configuration
@using NWDWebRuntime.Facades
@using NWDWebRuntime.Managers

@* @inject IViewLocalizer Localizer *@
@inject IOptions<RequestLocalizationOptions> LocOptions

@{
    string? tController = ViewContext.RouteData.Values["controller"]?.ToString();
    string? tAction = ViewContext.RouteData.Values["action"]?.ToString();
}

@inject IHttpContextAccessor HttpContextAccessor

    @if (HttpContextAccessor.HttpContext != null && NWDAuthorizeByAuthentication.ValidFor(HttpContextAccessor.HttpContext,true))
    {
        
        <div class="dropdown-divider"></div>
        <a class="dropdown-item text-secondary disabled" href="/"><span class="fas fa-chart-pie"></span> Player</a>
        <a class="dropdown-item" href="/"> Example</a>
        @* *@
        @* <li class="nav-item dropdown"> *@
        @*     <a class="nav-link dropdown-toggle" id="navbarDropdownAdmin" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false"> *@
        @*         Player *@
        @*     </a> *@
        @*     <div class="dropdown-menu dropdown-caret dropdown-caret dropdown-menu-end py-0" aria-labelledby="navbarDropdownAdmin"> *@
        @*         <div class="bg-white dark__bg-1000 rounded-2 py-2"> *@
        @*                 $1$ <a class="dropdown-item" href="@tAdminMenu.Asp_Controller()/@tAdminMenu.Asp_Action()/"> @tAdminMenu.MenuName()</a> #1# *@
        @*                 <a class="dropdown-item" href="/"> Example</a> *@
        @*             <div class="dropdown-divider"></div> *@
        @*             <span class="dropdown-item disabled">Engine by idéMobi©</span> *@
        @*         </div> *@
        @*     </div> *@
        @* </li> *@
    }