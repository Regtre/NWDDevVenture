

namespace UnitTest.NWDServer;

public class NWDTestSendEmail : NWDTestSetup
{
   /* [Test]
    public async Task TestSend()
    {
        NWDConfigurationerverEmail tSharedInstance = NWDConfigurationerverEmail.kSharedInstance;
        MailMessage tMessage = new MailMessage(tSharedInstance.SmtpUser, tSharedInstance.Contact, "Test is running!", "Test is running!");
        NWDSendEmail tSender = new NWDSendEmail();
        await tSender._SendMailAsync(tMessage);
        Assert.Pass("SendEmail:  Email was sent!");
    }*/
}
